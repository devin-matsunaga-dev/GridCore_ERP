{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "//": "Local development credentials only. They are checked in deliberately so a fresh clone can `aspire run` with no setup, and because every container above persists its users into a data volume — a per-run generated password would lock the AppHost out of the volume it created last time. Production supplies these parameters from the environment; appsettings.json (all environments) deliberately has no values.",
  "Parameters": {
    "postgres-username": "gridcore",
    "postgres-password": "gridcore-dev-password",
    "rabbitmq-username": "gridcore",
    "rabbitmq-password": "gridcore-dev-password",
    "keycloak-admin-username": "admin",
    "keycloak-admin-password": "gridcore-dev-password",
    "minio-access-key": "gridcore",
    "minio-secret-key": "gridcore-dev-password"
  }
}
