namespace GridCore.Contracts;

/// <summary>
/// Marker for the cross-module contract assembly. Holds events, shared value objects and
/// provider interfaces only — never entities or EF types.
/// </summary>
public static class AssemblyMarker;
