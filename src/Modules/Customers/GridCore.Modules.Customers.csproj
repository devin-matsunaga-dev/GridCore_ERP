<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>GridCore.Modules.Customers</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <!-- Design-time only: `dotnet ef migrations add` against this library. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <!--
    The fast tier proves an arrangement's allocation arithmetic (WP-2.20), which is deliberately not
    public API: only the service inside this module may move money onto a schedule, so the aggregate
    keeps `Apply` and `Settle` internal rather than opening them to every caller in the host.
  -->
  <ItemGroup>
    <InternalsVisibleTo Include="GridCore.Modules.Customers.UnitTests" />
  </ItemGroup>

  <!-- Modules reference Platform and Contracts only. A module NEVER references another module. -->
  <ItemGroup>
    <ProjectReference Include="../../Platform/GridCore.Platform.csproj" />
    <ProjectReference Include="../../Contracts/GridCore.Contracts.csproj" />
  </ItemGroup>

</Project>
