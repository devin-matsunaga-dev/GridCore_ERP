{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "//Authentication": "OIDC provider. The AppHost supplies Authority/Audience/RequireHttpsMetadata in development; production supplies the same keys from the environment. Swapping identity provider is a change of these values only — no code change.",
  "Authentication": {
    "Authority": "",
    "Audience": "gridcore-api",
    "RequireHttpsMetadata": true,
    "RolesClaimPath": "realm_access.roles",
    "NameClaimType": "preferred_username"
  }
}
